{
 "encodings": [
  {
   "cpuid": "",
   "layout": {
    "opcode": [
     "0F",
     "08"
    ]
   },
   "modes": {
    "legacy": "valid",
    "long": "valid"
   },
   "note": "Flush internal caches; initiate flushing of external caches.",
   "opcode": "0F 08",
   "operands": "ZO",
   "syntax": "INVD"
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": "#GP(0)",
   "when": "If the current privilege level is not 0. If the processor reserved memory protections are activated. If the SEAM range is protected. If CPUID.07H.01H:EAX[30] = 1 and bit 0 is set in MSR_BIOS_DONE (MSR address 151H)."
  },
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If the LOCK prefix is used."
  },
  {
   "mode": "real",
   "vector": "#GP(0)",
   "when": "If CPUID.07H.01H:EAX[30] = 1 and bit 0 is set in MSR_BIOS_DONE (MSR address 151H). If the processor reserved memory protections are activated. If the SEAM range is protected."
  },
  {
   "mode": "real",
   "vector": "#UD",
   "when": "If the LOCK prefix is used."
  },
  {
   "mode": "virtual8086",
   "vector": "#GP(0)",
   "when": "The INVD instruction cannot be executed in virtual-8086 mode."
  },
  {
   "mode": "compat",
   "vector": null,
   "when": "Same exceptions as in protected mode."
  },
  {
   "mode": "long",
   "vector": null,
   "when": "Same exceptions as in protected mode."
  }
 ],
 "format": "isadoc",
 "id": "invd",
 "isa": "x86",
 "links": {
  "arch_data": [
   "INVD"
  ]
 },
 "mnemonics": [
  "INVD"
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    1187,
    1188
   ]
  }
 ],
 "version": 1
}
