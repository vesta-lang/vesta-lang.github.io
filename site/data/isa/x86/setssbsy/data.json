{
 "encodings": [
  {
   "cpuid": "CET_SS",
   "layout": {
    "opcode": [
     "0F",
     "01",
     "E8"
    ],
    "prefixes": [
     "F3"
    ]
   },
   "modes": {
    "legacy": "v",
    "long": "v"
   },
   "note": "Set busy flag in supervisor shadow stack token reference by IA32_PL0_SSP.",
   "opcode": "F3 0F 01 E8",
   "operands": "ZO",
   "syntax": "SETSSBSY"
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If CR4.CET = 0. IF IA32_S_CET.SH_STK_EN = 0."
  },
  {
   "mode": "protected",
   "vector": "#GP(0)",
   "when": "If IA32_PL0_SSP not aligned to 8 bytes. If CPL is not 0."
  },
  {
   "mode": "protected",
   "vector": "#CP(setssbsy)",
   "when": "If busy bit in token is set."
  },
  {
   "mode": "protected",
   "vector": "#PF(fault-code)",
   "when": "If the address in token is not below 4 GBytes. If a page fault occurs."
  },
  {
   "mode": "real",
   "vector": "#UD",
   "when": "The SETSSBSY instruction is not recognized in real-address mode."
  },
  {
   "mode": "virtual8086",
   "vector": "#UD",
   "when": "The SETSSBSY instruction is not recognized in virtual-8086 mode."
  },
  {
   "mode": "compat",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If CR4.CET = 0. IF IA32_S_CET.SH_STK_EN = 0. If CR4.FRED = 1."
  },
  {
   "mode": "compat",
   "vector": "#GP(0)",
   "when": "If IA32_PL0_SSP not aligned to 8 bytes. If CPL is not 0."
  },
  {
   "mode": "compat",
   "vector": "#CP(setssbsy)",
   "when": "If busy bit in token is set. If the address in token is not below 4 GBytes. #PF(fault-code) If a page fault occurs."
  },
  {
   "mode": "long",
   "vector": null,
   "when": "Same as compatibility mode exceptions (except there is no check on the token address)."
  }
 ],
 "format": "isadoc",
 "id": "setssbsy",
 "isa": "x86",
 "links": {
  "arch_data": [
   "SETSSBSY"
  ]
 },
 "mnemonics": [
  "SETSSBSY"
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    1914,
    1915
   ]
  }
 ],
 "version": 1
}
