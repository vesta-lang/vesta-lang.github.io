{
 "encodings": [
  {
   "cpuid": "CET_SS",
   "layout": {
    "modrm": {
     "kind": "digit",
     "mod": "11",
     "negated": true,
     "value": 5
    },
    "opcode": [
     "0F",
     "01"
    ],
    "prefixes": [
     "F3"
    ]
   },
   "modes": {
    "legacy": "v",
    "long": "v"
   },
   "note": "Restore SSP.",
   "opcode": "F3 0F 01 /5 (mod!=11, /5, memory only)",
   "operands": "M",
   "syntax": "RSTORSSP m64"
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If CR4.CET = 0. IF CPL = 3 and IA32_U_CET.SH_STK_EN = 0. IF CPL < 3 and IA32_S_CET.SH_STK_EN = 0."
  },
  {
   "mode": "protected",
   "vector": "#GP(0)",
   "when": "If linear address of memory operand not 8 byte aligned. If a memory operand effective address is outside the CS, DS, ES, FS, or GS segment limit. If destination is located in a non-writeable segment. If the DS, ES, FS, or GS register is used to access memory and it contains a NULL segment selector."
  },
  {
   "mode": "protected",
   "vector": "#SS(0)",
   "when": "If a memory operand effective address is outside the SS segment limit."
  },
  {
   "mode": "protected",
   "vector": "#CP(rstorssp)",
   "when": "If L bit in token does not match (IA32_EFER.LMA & CS.L). If address in token does not match linear address of memory operand. If in 32-bit or compatibility mode and the address in token is not below 4G. #PF(fault-code) If a page fault occurs."
  },
  {
   "mode": "real",
   "vector": "#UD",
   "when": "The RSTORSSP instruction is not recognized in real-address mode."
  },
  {
   "mode": "virtual8086",
   "vector": "#UD",
   "when": "The RSTORSSP instruction is not recognized in virtual-8086 mode."
  },
  {
   "mode": "compat",
   "vector": null,
   "when": "Same as protected mode exceptions."
  },
  {
   "mode": "long",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If CR4.CET = 0. If CPL = 3 and IA32_U_CET.SH_STK_EN = 0. If CPL < 3 and IA32_S_CET.SH_STK_EN = 0."
  },
  {
   "mode": "long",
   "vector": "#GP(0)",
   "when": "If linear address of memory operand not 8 byte aligned. If a memory address is in a non-canonical form."
  },
  {
   "mode": "long",
   "vector": "#SS(0)",
   "when": "If a memory address referencing the SS segment is in a non-canonical form."
  },
  {
   "mode": "long",
   "vector": "#CP(rstorssp)",
   "when": "If L bit in token does not match (IA32_EFER.LMA & CS.L). If address in token does not match linear address of memory operand. #PF(fault-code) If a page fault occurs."
  }
 ],
 "flags": [
  "CF",
  "PF",
  "AF",
  "ZF",
  "SF",
  "OF"
 ],
 "format": "isadoc",
 "id": "rstorssp",
 "isa": "x86",
 "links": {
  "arch_data": [
   "RSTORSSP"
  ]
 },
 "mnemonics": [
  "RSTORSSP"
 ],
 "operand_encodings": [
  {
   "id": "M",
   "operands": [
    {
     "field": "modrm.rm",
     "read": true,
     "write": true
    }
   ]
  }
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    1887,
    1889
   ]
  }
 ],
 "version": 1
}
