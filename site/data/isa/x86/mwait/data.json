{
 "encodings": [
  {
   "cpuid": "",
   "layout": {
    "opcode": [
     "0F",
     "01",
     "C9"
    ]
   },
   "modes": {
    "legacy": "valid",
    "long": "valid"
   },
   "note": "A hint that allows the processor to stop instruction execution and enter an implementation-dependent optimized state until occurrence of a class of events.",
   "opcode": "0F 01 C9",
   "operands": "ZO",
   "syntax": "MWAIT"
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": "#GP(0)",
   "when": "If ECX[31:1]  0. If ECX[0] = 1 and CPUID.05H:ECX[1] = 0."
  },
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If CPUID.01H:ECX.MONITOR[3] = 0. If current privilege level is not 0. Real Address Mode Exceptions"
  },
  {
   "mode": "protected",
   "vector": "#GP",
   "when": "If ECX[31:1]  0. If ECX[0] = 1 and CPUID.05H:ECX[1] = 0."
  },
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If CPUID.01H:ECX.MONITOR[3] = 0. Virtual 8086 Mode Exceptions #UDThe MWAIT instruction is not recognized in virtual-8086 mode (even if CPUID.01H:ECX.MONITOR[3] = 1)."
  },
  {
   "mode": "compat",
   "vector": null,
   "when": "Same exceptions as in protected mode."
  },
  {
   "mode": "long",
   "vector": "#GP(0)",
   "when": "If RCX[63:1]  0. If RCX[0] = 1 and CPUID.05H:ECX[1] = 0."
  },
  {
   "mode": "long",
   "vector": "#UD",
   "when": "If the current privilege level is not 0. If CPUID.01H:ECX.MONITOR[3] = 0."
  }
 ],
 "format": "isadoc",
 "id": "mwait",
 "isa": "x86",
 "links": {
  "arch_data": [
   "MWAIT"
  ]
 },
 "mnemonics": [
  "MWAIT"
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    1444,
    1446
   ]
  }
 ],
 "version": 1
}
