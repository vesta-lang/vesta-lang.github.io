{
 "encodings": [
  {
   "cpuid": "CET_SS",
   "layout": {
    "modrm": {
     "kind": "reg",
     "mod": "11",
     "negated": true
    },
    "opcode": [
     "0F",
     "38",
     "F5"
    ],
    "prefixes": [
     "66"
    ]
   },
   "modes": {
    "legacy": "v",
    "long": "v"
   },
   "note": "Write 4 bytes to shadow stack.",
   "opcode": "66 0F 38 F5 !(11):rrr:bbb",
   "operands": "MR",
   "syntax": "WRUSSD m32, r32"
  },
  {
   "cpuid": "CET_SS",
   "layout": {
    "modrm": {
     "kind": "reg",
     "mod": "11",
     "negated": true
    },
    "opcode": [
     "0F",
     "38",
     "F5"
    ],
    "prefixes": [
     "66"
    ],
    "rex": "W"
   },
   "modes": {
    "legacy": "n.e.",
    "long": "v"
   },
   "note": "Write 8 bytes to shadow stack.",
   "opcode": "66 REX.W 0F 38 F5 !(11):rrr:bbb",
   "operands": "MR",
   "syntax": "WRUSSQ m64, r64"
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If CR4.CET = 0."
  },
  {
   "mode": "protected",
   "vector": "#GP(0)",
   "when": "If a memory operand effective address is outside the CS, DS, ES, FS, or GS segment limit. If destination is located in a non-writeable segment. If the DS, ES, FS, or GS register is used to access memory and it contains a NULL segment selector. If linear address of destination is not 4 byte aligned. If CPL is not 0."
  },
  {
   "mode": "protected",
   "vector": "#SS(0)",
   "when": "If a memory operand effective address is outside the SS segment limit. #PF(fault-code) If destination is not a user shadow stack. Other terminal and non-terminal faults."
  },
  {
   "mode": "real",
   "vector": "#UD",
   "when": "The WRUSS instruction is not recognized in real-address mode."
  },
  {
   "mode": "virtual8086",
   "vector": "#UD",
   "when": "The WRUSS instruction is not recognized in virtual-8086 mode."
  },
  {
   "mode": "compat",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If CR4.CET = 0."
  },
  {
   "mode": "compat",
   "vector": "#GP(0)",
   "when": "If a memory address is in a non-canonical form. If linear address of destination is not 4 byte aligned. If CPL is not 0."
  },
  {
   "mode": "compat",
   "vector": "#SS(0)",
   "when": "If a memory address referencing the SS segment is in a non-canonical form. #PF(fault-code) If destination is not a user shadow stack. Other terminal and non-terminal faults."
  },
  {
   "mode": "long",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If CR4.CET = 0."
  },
  {
   "mode": "long",
   "vector": "#GP(0)",
   "when": "If a memory address is in a non-canonical form. If linear address of destination is not 4 byte aligned. If CPL is not 0. #PF(fault-code) If destination is not a user shadow stack. Other terminal and non-terminal faults."
  }
 ],
 "format": "isadoc",
 "id": "wrussd",
 "isa": "x86",
 "links": {
  "arch_data": [
   "WRUSSD",
   "WRUSSQ"
  ]
 },
 "mnemonics": [
  "WRUSSD",
  "WRUSSQ"
 ],
 "operand_encodings": [
  {
   "id": "MR",
   "operands": [
    {
     "field": "modrm.rm",
     "read": false,
     "write": true
    },
    {
     "field": "modrm.reg",
     "read": true,
     "write": false
    }
   ]
  }
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    2846,
    2847
   ]
  }
 ],
 "version": 1
}
