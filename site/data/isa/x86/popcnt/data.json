{
 "encodings": [
  {
   "cpuid": "",
   "layout": {
    "modrm": {
     "kind": "reg"
    },
    "opcode": [
     "0F",
     "B8"
    ],
    "prefixes": [
     "F3"
    ]
   },
   "modes": {
    "legacy": "valid",
    "long": "valid"
   },
   "note": "POPCNT on r/m16",
   "opcode": "F3 0F B8 /r",
   "operands": "RM",
   "syntax": "POPCNT r16, r/m16"
  },
  {
   "cpuid": "",
   "layout": {
    "modrm": {
     "kind": "reg"
    },
    "opcode": [
     "0F",
     "B8"
    ],
    "prefixes": [
     "F3"
    ]
   },
   "modes": {
    "legacy": "valid",
    "long": "valid"
   },
   "note": "POPCNT on r/m32",
   "opcode": "F3 0F B8 /r",
   "operands": "RM",
   "syntax": "POPCNT r32, r/m32"
  },
  {
   "cpuid": "",
   "layout": {
    "modrm": {
     "kind": "reg"
    },
    "opcode": [
     "0F",
     "B8"
    ],
    "prefixes": [
     "F3"
    ],
    "rex": "W"
   },
   "modes": {
    "legacy": "n.e.",
    "long": "valid"
   },
   "note": "POPCNT on r/m64",
   "opcode": "F3 REX.W 0F B8 /r",
   "operands": "RM",
   "syntax": "POPCNT r64, r/m64"
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": "#GP(0)",
   "when": "If a memory operand effective address is outside the CS, DS, ES, FS or GS segments."
  },
  {
   "mode": "protected",
   "vector": "#SS(0)",
   "when": "If a memory operand effective address is outside the SS segment limit. #PF (fault-code) For a page fault."
  },
  {
   "mode": "protected",
   "vector": "#AC(0)",
   "when": "If an unaligned memory reference is made while the current privilege level is 3 and alignment checking is enabled."
  },
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If CPUID.01H:ECX.POPCNT[23] = 0. If LOCK prefix is used."
  },
  {
   "mode": "real",
   "vector": "#GP(0)",
   "when": "If any part of the operand lies outside of the effective address space from 0 to 0FFFFH."
  },
  {
   "mode": "real",
   "vector": "#SS(0)",
   "when": "If a memory operand effective address is outside the SS segment limit."
  },
  {
   "mode": "real",
   "vector": "#UD",
   "when": "If CPUID.01H:ECX.POPCNT[23] = 0. If LOCK prefix is used. Virtual 8086 Mode Exceptions"
  },
  {
   "mode": "real",
   "vector": "#GP(0)",
   "when": "If any part of the operand lies outside of the effective address space from 0 to 0FFFFH."
  },
  {
   "mode": "real",
   "vector": "#SS(0)",
   "when": "If a memory operand effective address is outside the SS segment limit. #PF (fault-code) For a page fault."
  },
  {
   "mode": "real",
   "vector": "#AC(0)",
   "when": "If an unaligned memory reference is made while alignment checking is enabled."
  },
  {
   "mode": "real",
   "vector": "#UD",
   "when": "If CPUID.01H:ECX.POPCNT[23] = 0. If LOCK prefix is used."
  },
  {
   "mode": "compat",
   "vector": null,
   "when": "Same exceptions as in Protected Mode."
  },
  {
   "mode": "long",
   "vector": "#GP(0)",
   "when": "If the memory address is in a non-canonical form."
  },
  {
   "mode": "long",
   "vector": "#SS(0)",
   "when": "If a memory address referencing the SS segment is in a non-canonical form. #PF (fault-code) For a page fault."
  },
  {
   "mode": "long",
   "vector": "#AC(0)",
   "when": "If alignment checking is enabled and an unaligned memory reference is made while the current privilege level is 3."
  },
  {
   "mode": "long",
   "vector": "#UD",
   "when": "If CPUID.01H:ECX.POPCNT[23] = 0. If LOCK prefix is used."
  }
 ],
 "flags": [
  "CF",
  "PF",
  "AF",
  "ZF",
  "SF",
  "OF"
 ],
 "format": "isadoc",
 "id": "popcnt",
 "isa": "x86",
 "links": {
  "arch_data": [
   "POPCNT"
  ]
 },
 "mnemonics": [
  "POPCNT"
 ],
 "operand_encodings": [
  {
   "id": "RM",
   "operands": [
    {
     "field": "modrm.reg",
     "read": false,
     "write": true
    },
    {
     "field": "modrm.rm",
     "read": true,
     "write": false
    }
   ]
  }
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    1694,
    1695
   ]
  }
 ],
 "version": 1
}
