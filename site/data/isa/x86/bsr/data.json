{
 "encodings": [
  {
   "cpuid": "",
   "layout": {
    "modrm": {
     "kind": "reg"
    },
    "opcode": [
     "0F",
     "BD"
    ]
   },
   "modes": {
    "legacy": "valid",
    "long": "valid"
   },
   "note": "Bit scan reverse on r/m16.",
   "opcode": "0F BD /r",
   "operands": "RM",
   "syntax": "BSR r16, r/m16"
  },
  {
   "cpuid": "",
   "layout": {
    "modrm": {
     "kind": "reg"
    },
    "opcode": [
     "0F",
     "BD"
    ]
   },
   "modes": {
    "legacy": "valid",
    "long": "valid"
   },
   "note": "Bit scan reverse on r/m32.",
   "opcode": "0F BD /r",
   "operands": "RM",
   "syntax": "BSR r32, r/m32"
  },
  {
   "cpuid": "",
   "layout": {
    "modrm": {
     "kind": "reg"
    },
    "opcode": [
     "0F",
     "BD"
    ],
    "rex": "W"
   },
   "modes": {
    "legacy": "n.e.",
    "long": "valid"
   },
   "note": "Bit scan reverse on r/m64.",
   "opcode": "REX.W + 0F BD /r",
   "operands": "RM",
   "syntax": "BSR r64, r/m64"
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": "#GP(0)",
   "when": "If a memory operand effective address is outside the CS, DS, ES, FS, or GS segment limit. If the DS, ES, FS, or GS register contains a NULL segment selector."
  },
  {
   "mode": "protected",
   "vector": "#SS(0)",
   "when": "If a memory operand effective address is outside the SS segment limit. #PF(fault-code) If a page fault occurs."
  },
  {
   "mode": "protected",
   "vector": "#AC(0)",
   "when": "If alignment checking is enabled and an unaligned memory reference is made while the current privilege level is 3."
  },
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. 1. With a zero source operand on some older processors, use of a 32-bit operand size may clear the upper 32 bits of a 64-bit destination while leaving the lower 32 bits unmodified. 2. On some older processors, the CF, OF, SF, AF, and PF flags are unmodified."
  },
  {
   "mode": "real",
   "vector": "#GP",
   "when": "If a memory operand effective address is outside the CS, DS, ES, FS, or GS segment limit."
  },
  {
   "mode": "real",
   "vector": "#SS",
   "when": "If a memory operand effective address is outside the SS segment limit."
  },
  {
   "mode": "real",
   "vector": "#UD",
   "when": "If the LOCK prefix is used."
  },
  {
   "mode": "virtual8086",
   "vector": "#GP(0)",
   "when": "If a memory operand effective address is outside the CS, DS, ES, FS, or GS segment limit."
  },
  {
   "mode": "virtual8086",
   "vector": "#SS(0)",
   "when": "If a memory operand effective address is outside the SS segment limit. #PF(fault-code) If a page fault occurs."
  },
  {
   "mode": "virtual8086",
   "vector": "#AC(0)",
   "when": "If alignment checking is enabled and an unaligned memory reference is made."
  },
  {
   "mode": "virtual8086",
   "vector": "#UD",
   "when": "If the LOCK prefix is used."
  },
  {
   "mode": "compat",
   "vector": null,
   "when": "Same exceptions as in protected mode."
  },
  {
   "mode": "long",
   "vector": "#SS(0)",
   "when": "If a memory address referencing the SS segment is in a non-canonical form."
  },
  {
   "mode": "long",
   "vector": "#GP(0)",
   "when": "If the memory address is in a non-canonical form. #PF(fault-code) If a page fault occurs."
  },
  {
   "mode": "long",
   "vector": "#AC(0)",
   "when": "If alignment checking is enabled and an unaligned memory reference is made while the current privilege level is 3."
  },
  {
   "mode": "long",
   "vector": "#UD",
   "when": "If the LOCK prefix is used."
  }
 ],
 "flags": [
  "CF",
  "PF",
  "AF",
  "ZF",
  "SF",
  "OF"
 ],
 "format": "isadoc",
 "id": "bsr",
 "isa": "x86",
 "links": {
  "arch_data": [
   "BSR"
  ]
 },
 "mnemonics": [
  "BSR"
 ],
 "operand_encodings": [
  {
   "id": "RM",
   "operands": [
    {
     "field": "modrm.reg",
     "read": false,
     "write": true
    },
    {
     "field": "modrm.rm",
     "read": true,
     "write": false
    }
   ]
  }
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    813,
    814
   ]
  }
 ],
 "version": 1
}
