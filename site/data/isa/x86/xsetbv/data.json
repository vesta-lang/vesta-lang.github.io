{
 "encodings": [
  {
   "cpuid": "",
   "layout": {
    "opcode": [
     "0F",
     "01",
     "D1"
    ],
    "prefixes": [
     "NP"
    ]
   },
   "modes": {
    "legacy": "valid",
    "long": "valid"
   },
   "note": "Write the value in EDX:EAX to the XCR specified by ECX.",
   "opcode": "NP 0F 01 D1",
   "operands": "ZO",
   "syntax": "XSETBV"
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": "#GP(0)",
   "when": "If the current privilege level is not 0. If an invalid XCR is specified in ECX. If the value in EDX:EAX sets bits that are reserved in the XCR specified by ECX. If an attempt is made to clear bit 0 of XCR0. If an attempt is made to set XCR0[2:1] to 10b."
  },
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If CPUID.01H:ECX.XSAVE[26] = 0. If CR4.OSXSAVE[bit 18] = 0. If the LOCK prefix is used."
  },
  {
   "mode": "real",
   "vector": "#GP",
   "when": "If an invalid XCR is specified in ECX. If the value in EDX:EAX sets bits that are reserved in the XCR specified by ECX. If an attempt is made to clear bit 0 of XCR0. If an attempt is made to set XCR0[2:1] to 10b."
  },
  {
   "mode": "real",
   "vector": "#UD",
   "when": "If CPUID.01H:ECX.XSAVE[26] = 0. If CR4.OSXSAVE[bit 18] = 0. If the LOCK prefix is used."
  },
  {
   "mode": "virtual8086",
   "vector": "#GP(0)",
   "when": "The XSETBV instruction is not recognized in virtual-8086 mode."
  },
  {
   "mode": "compat",
   "vector": null,
   "when": "Same exceptions as in protected mode."
  },
  {
   "mode": "long",
   "vector": null,
   "when": "Same exceptions as in protected mode."
  }
 ],
 "format": "isadoc",
 "id": "xsetbv",
 "isa": "x86",
 "links": {
  "arch_data": [
   "XSETBV"
  ]
 },
 "mnemonics": [
  "XSETBV"
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    2897,
    2898
   ]
  }
 ],
 "version": 1
}
