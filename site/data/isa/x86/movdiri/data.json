{
 "encodings": [
  {
   "cpuid": "MOVDIRI",
   "layout": {
    "modrm": {
     "kind": "reg"
    },
    "opcode": [
     "0F",
     "38",
     "F9"
    ],
    "prefixes": [
     "NP"
    ]
   },
   "modes": {
    "legacy": "v",
    "long": "v"
   },
   "note": "Move doubleword from r32 to m32 using direct store.",
   "opcode": "NP 0F 38 F9 /r",
   "operands": "A",
   "syntax": "MOVDIRI m32, r32"
  },
  {
   "cpuid": "MOVDIRI",
   "layout": {
    "modrm": {
     "kind": "reg"
    },
    "opcode": [
     "0F",
     "38",
     "F9"
    ],
    "prefixes": [
     "NP"
    ],
    "rex": "W"
   },
   "modes": {
    "legacy": "n.e.",
    "long": "v"
   },
   "note": "Move quadword from r64 to m64 using direct store. Instruction Operand Encod ing1 Op erand 2 Operand 3   Operand 4 Mo dRM:reg (r) N/A  N/A",
   "opcode": "NP REX.W + 0F 38 F9    /r",
   "operands": "A",
   "syntax": "MOVDIRI m64, r64 Op/En       Tuple       Operand 1 A           N/A        ModRM:r/m (w)"
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": "#GP(0)",
   "when": "For an illegal memory operand effective address in the CS, DS, ES, FS or GS segments."
  },
  {
   "mode": "protected",
   "vector": "#SS(0)",
   "when": "For an illegal address in the SS segment. #PF (fault-code) For a page fault."
  },
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If CPUID.07H.00H:ECX.MOVDIRI[27] = 0. If LOCK prefix or operand-size (66H) prefix is used."
  },
  {
   "mode": "protected",
   "vector": "#AC",
   "when": "If alignment checking is enabled and an unaligned memory reference made while in current privilege level 3."
  },
  {
   "mode": "real",
   "vector": "#GP",
   "when": "If any part of the operand lies outside the effective address space from 0 to FFFFH."
  },
  {
   "mode": "real",
   "vector": "#UD",
   "when": "If CPUID.07H.00H:ECX.MOVDIRI[27] = 0. If LOCK prefix or operand-size (66H) prefix is used."
  },
  {
   "mode": "virtual8086",
   "vector": null,
   "when": "Same exceptions as in real address mode."
  },
  {
   "mode": "virtual8086",
   "vector": "#AC",
   "when": "If alignment checking is enabled and an unaligned memory reference made while in current privilege level 3."
  },
  {
   "mode": "compat",
   "vector": null,
   "when": "Same exceptions as in protected mode."
  },
  {
   "mode": "long",
   "vector": "#SS(0)",
   "when": "If memory address referencing the SS segment is in non-canonical form."
  },
  {
   "mode": "long",
   "vector": "#GP(0)",
   "when": "If the memory address is in non-canonical form. #PF (fault-code) For a page fault."
  },
  {
   "mode": "long",
   "vector": "#UD",
   "when": "If CPUID.07H.00H:ECX.MOVDIRI[27] = 0. If LOCK prefix or operand-size (66H) prefix is used."
  },
  {
   "mode": "long",
   "vector": "#AC",
   "when": "If alignment checking is enabled and an unaligned memory reference made while in current privilege level 3."
  }
 ],
 "format": "isadoc",
 "id": "movdiri",
 "isa": "x86",
 "links": {
  "arch_data": [
   "MOVDIRI"
  ]
 },
 "mnemonics": [
  "MOVDIRI"
 ],
 "operand_encodings": [
  {
   "id": "A",
   "operands": [
    {
     "field": "modrm.rm",
     "read": false,
     "write": true
    },
    {
     "field": "modrm.reg",
     "read": true,
     "write": false
    }
   ]
  }
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    1342,
    1343
   ]
  }
 ],
 "version": 1
}
