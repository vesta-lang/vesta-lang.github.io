{
 "encodings": [
  {
   "cpuid": "AES_WIDE",
   "layout": {
    "modrm": {
     "kind": "reg",
     "mod": "11",
     "negated": true,
     "reg": "011"
    },
    "opcode": [
     "0F",
     "38",
     "D8"
    ],
    "prefixes": [
     "F3"
    ]
   },
   "modes": {
    "legacy": "v",
    "long": "v"
   },
   "note": "Decrypt XMM0-7 using 256-bit AES key indicated by handle at m512 and store each resultant block back to its corresponding register.",
   "opcode": "F3 0F 38 D8 !(11):011:bbb",
   "operands": "A",
   "syntax": "AESDECWIDE256KL m512, <XMM0-7>"
  }
 ],
 "exceptions": [
  {
   "mode": "real",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If CPUID.07H.00H:ECX.KEY_LOCKER[23] = 0. If CR4.KL = 0. If CPUID.19H:EBX.AESKLE[0] = 0. If CR0.EM = 1. If CR4.OSFXSR = 0. If CPUID.19H:EBX.AES_WIDE[2] = 0."
  },
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If CPUID.07H.00H:ECX.KEY_LOCKER[23] = 0. If CR4.KL = 0. If CPUID.19H:EBX.AESKLE[0] = 0. If CR0.EM = 1. If CR4.OSFXSR = 0. If CPUID.19H:EBX.AES_WIDE[2] = 0."
  },
  {
   "mode": "virtual8086",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If CPUID.07H.00H:ECX.KEY_LOCKER[23] = 0. If CR4.KL = 0. If CPUID.19H:EBX.AESKLE[0] = 0. If CR0.EM = 1. If CR4.OSFXSR = 0. If CPUID.19H:EBX.AES_WIDE[2] = 0."
  },
  {
   "mode": "compat",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If CPUID.07H.00H:ECX.KEY_LOCKER[23] = 0. If CR4.KL = 0. If CPUID.19H:EBX.AESKLE[0] = 0. If CR0.EM = 1. If CR4.OSFXSR = 0. If CPUID.19H:EBX.AES_WIDE[2] = 0."
  },
  {
   "mode": "long",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If CPUID.07H.00H:ECX.KEY_LOCKER[23] = 0. If CR4.KL = 0. If CPUID.19H:EBX.AESKLE[0] = 0. If CR0.EM = 1. If CR4.OSFXSR = 0. If CPUID.19H:EBX.AES_WIDE[2] = 0."
  },
  {
   "mode": "real",
   "vector": "#NM",
   "when": "If CR0.TS = 1."
  },
  {
   "mode": "protected",
   "vector": "#NM",
   "when": "If CR0.TS = 1."
  },
  {
   "mode": "virtual8086",
   "vector": "#NM",
   "when": "If CR0.TS = 1."
  },
  {
   "mode": "compat",
   "vector": "#NM",
   "when": "If CR0.TS = 1."
  },
  {
   "mode": "long",
   "vector": "#NM",
   "when": "If CR0.TS = 1."
  },
  {
   "mode": "real",
   "vector": "#PF",
   "when": "If a page fault occurs."
  },
  {
   "mode": "protected",
   "vector": "#PF",
   "when": "If a page fault occurs."
  },
  {
   "mode": "virtual8086",
   "vector": "#PF",
   "when": "If a page fault occurs."
  },
  {
   "mode": "compat",
   "vector": "#PF",
   "when": "If a page fault occurs."
  },
  {
   "mode": "long",
   "vector": "#PF",
   "when": "If a page fault occurs."
  },
  {
   "mode": "real",
   "vector": "#GP(0)",
   "when": "If a memory operand effective address is outside the CS, DS, ES, FS, or GS segment limit. If the DS, ES, FS, or GS register is used to access memory and it contains a NULL segment selector. If the memory address is in a non-canonical form."
  },
  {
   "mode": "protected",
   "vector": "#GP(0)",
   "when": "If a memory operand effective address is outside the CS, DS, ES, FS, or GS segment limit. If the DS, ES, FS, or GS register is used to access memory and it contains a NULL segment selector. If the memory address is in a non-canonical form."
  },
  {
   "mode": "virtual8086",
   "vector": "#GP(0)",
   "when": "If a memory operand effective address is outside the CS, DS, ES, FS, or GS segment limit. If the DS, ES, FS, or GS register is used to access memory and it contains a NULL segment selector. If the memory address is in a non-canonical form."
  },
  {
   "mode": "compat",
   "vector": "#GP(0)",
   "when": "If a memory operand effective address is outside the CS, DS, ES, FS, or GS segment limit. If the DS, ES, FS, or GS register is used to access memory and it contains a NULL segment selector. If the memory address is in a non-canonical form."
  },
  {
   "mode": "long",
   "vector": "#GP(0)",
   "when": "If a memory operand effective address is outside the CS, DS, ES, FS, or GS segment limit. If the DS, ES, FS, or GS register is used to access memory and it contains a NULL segment selector. If the memory address is in a non-canonical form."
  },
  {
   "mode": "real",
   "vector": "#SS(0)",
   "when": "If a memory operand effective address is outside the SS segment limit. If a memory address referencing the SS segment is in a non-canonical form."
  },
  {
   "mode": "protected",
   "vector": "#SS(0)",
   "when": "If a memory operand effective address is outside the SS segment limit. If a memory address referencing the SS segment is in a non-canonical form."
  },
  {
   "mode": "virtual8086",
   "vector": "#SS(0)",
   "when": "If a memory operand effective address is outside the SS segment limit. If a memory address referencing the SS segment is in a non-canonical form."
  },
  {
   "mode": "compat",
   "vector": "#SS(0)",
   "when": "If a memory operand effective address is outside the SS segment limit. If a memory address referencing the SS segment is in a non-canonical form."
  },
  {
   "mode": "long",
   "vector": "#SS(0)",
   "when": "If a memory operand effective address is outside the SS segment limit. If a memory address referencing the SS segment is in a non-canonical form."
  }
 ],
 "flags": [
  "CF",
  "PF",
  "AF",
  "ZF",
  "SF",
  "OF"
 ],
 "format": "isadoc",
 "id": "aesdecwide256kl",
 "isa": "x86",
 "links": {
  "arch_data": [
   "AESDECWIDE256KL"
  ]
 },
 "mnemonics": [
  "AESDECWIDE256KL"
 ],
 "operand_encodings": [
  {
   "id": "A",
   "operands": [
    {
     "field": "modrm.rm",
     "read": true,
     "write": false
    },
    {
     "field": "XMM0-7",
     "implicit": true,
     "raw": "XMM0-7",
     "read": true,
     "write": true
    }
   ]
  }
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    747,
    748
   ]
  }
 ],
 "version": 1
}
