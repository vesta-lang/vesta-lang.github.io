{
 "encodings": [
  {
   "cpuid": "",
   "layout": {
    "opcode": [
     "FA"
    ]
   },
   "modes": {
    "legacy": "valid",
    "long": "valid"
   },
   "note": "Clear interrupt flag; interrupts disabled when interrupt flag cleared.",
   "opcode": "FA",
   "operands": "ZO",
   "syntax": "CLI"
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": "#GP(0)",
   "when": "If CPL is greater than IOPL and PVI mode is not active. If CPL is greater than IOPL and less than 3."
  },
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If the LOCK prefix is used."
  },
  {
   "mode": "real",
   "vector": "#UD",
   "when": "If the LOCK prefix is used."
  },
  {
   "mode": "virtual8086",
   "vector": "#GP(0)",
   "when": "If IOPL is less than 3 and VME mode is not active."
  },
  {
   "mode": "virtual8086",
   "vector": "#UD",
   "when": "If the LOCK prefix is used."
  },
  {
   "mode": "compat",
   "vector": null,
   "when": "Same exceptions as in protected mode."
  },
  {
   "mode": "long",
   "vector": null,
   "when": "Same exceptions as in protected mode."
  }
 ],
 "flags": [
  "IF"
 ],
 "format": "isadoc",
 "id": "cli",
 "isa": "x86",
 "links": {
  "arch_data": [
   "CLI"
  ]
 },
 "mnemonics": [
  "CLI"
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    852,
    853
   ]
  }
 ],
 "version": 1
}
