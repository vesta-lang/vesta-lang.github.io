{
 "encodings": [
  {
   "cpuid": "",
   "layout": {
    "opcode": [
     "CC"
    ]
   },
   "modes": {
    "legacy": "valid",
    "long": "valid"
   },
   "note": "Generate breakpoint trap.",
   "opcode": "CC",
   "operands": "ZO",
   "syntax": "INT3"
  },
  {
   "cpuid": "",
   "layout": {
    "immediates": [
     {
      "bytes": 1,
      "marker": "ib"
     }
    ],
    "opcode": [
     "CD"
    ]
   },
   "modes": {
    "legacy": "valid",
    "long": "valid"
   },
   "note": "Generate software interrupt with vector specified by immediate byte.",
   "opcode": "CD ib",
   "operands": "I",
   "syntax": "INT imm8"
  },
  {
   "cpuid": "",
   "layout": {
    "opcode": [
     "CE"
    ]
   },
   "modes": {
    "legacy": "valid",
    "long": "invalid"
   },
   "note": "Generate overflow trap if overflow flag is 1.",
   "opcode": "CE",
   "operands": "ZO",
   "syntax": "INTO"
  },
  {
   "cpuid": "",
   "layout": {
    "opcode": [
     "F1"
    ]
   },
   "modes": {
    "legacy": "valid",
    "long": "valid"
   },
   "note": "Generate debug trap.",
   "opcode": "F1",
   "operands": "ZO",
   "syntax": "INT1"
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": null,
   "when": "#GP(error_code) If the instruction pointer in the IDT or in the interrupt, trap, or task gate is beyond the code"
  },
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If the LOCK prefix is used."
  },
  {
   "mode": "protected",
   "vector": "#AC(EXT)",
   "when": "If alignment checking is enabled, the gate DPL is 3, and a stack push is unaligned."
  },
  {
   "mode": "real",
   "vector": "#GP",
   "when": "If a memory operand effective address is outside the CS, DS, ES, FS, or GS segment limit. If the interrupt vector number is outside the IDT limits."
  },
  {
   "mode": "real",
   "vector": "#SS",
   "when": "If stack limit violation on push. If pushing the return address, flags, or error code onto the stack exceeds the bounds of the stack segment."
  },
  {
   "mode": "real",
   "vector": "#UD",
   "when": "If the LOCK prefix is used."
  },
  {
   "mode": "virtual8086",
   "vector": null,
   "when": "#GP(error_code) (For INT n, INTO, or BOUND instruction) If the IOPL is less than 3 or the DPL of the interrupt,"
  },
  {
   "mode": "virtual8086",
   "vector": "#SS(error_code)",
   "when": "If the instruction pointer in the IDT or in the interrupt, trap, or task gate is beyond the code segment limits."
  },
  {
   "mode": "virtual8086",
   "vector": "#NP(error_code)",
   "when": "If the segment selector in the interrupt, trap, or task gate is NULL."
  },
  {
   "mode": "virtual8086",
   "vector": "#TS(error_code)",
   "when": "If a interrupt gate, trap gate, task gate, code segment, or TSS segment selector index is outside its descriptor table limits."
  },
  {
   "mode": "virtual8086",
   "vector": "#PF(fault-code)",
   "when": "If the vector selects a descriptor outside the IDT limits."
  },
  {
   "mode": "virtual8086",
   "vector": "#OF",
   "when": "If an IDT descriptor is not an interrupt, trap, or task gate."
  },
  {
   "mode": "virtual8086",
   "vector": "#UD",
   "when": "If an interrupt is generated by INT n, INT3, or INTO and the DPL of an interrupt, trap, or task"
  },
  {
   "mode": "virtual8086",
   "vector": "#AC(EXT)",
   "when": "gate is less than the CPL. If the segment selector in an interrupt or trap gate does not point to a segment descriptor for a code segment. If the segment selector for a TSS has its local/global bit set for local. If the SS register is being loaded and the segment pointed to is marked not present. If pushing the return address, flags, error code, stack segment pointer, or data segments exceeds the bounds of the stack segment. If code segment, interrupt gate, trap gate, task gate, or TSS is not present. If the RPL of the stack segment selector in the TSS is not equal to the DPL of the code segment being accessed by the interrupt or trap gate. If DPL of the stack segment descriptor for the TSS's stack segment is not equal to the DPL of the code segment descriptor for the interrupt or trap gate. If the stack segment selector in the TSS is NULL. If the stack segment for the TSS is not a writable data segment. If segment-selector index for stack segment is outside descriptor table limits. If a page fault occurs. If the INTO instruction is executed and the OF flag is set. If the LOCK prefix is used. If alignment checking is enabled, the gate DPL is 3, and a stack push is unaligned."
  },
  {
   "mode": "compat",
   "vector": null,
   "when": "#GP(error_code) If the instruction pointer in the 64-bit interrupt gate or trap gate is non-canonical."
  },
  {
   "mode": "compat",
   "vector": "#GP(0)",
   "when": "If SSP in IA32_PLi_SSP (where i is the new CPL) is not 8 byte aligned. If the token and the stack frame to be pushed on shadow stack are not contained in a naturally aligned 32-byte region of the shadow stack. If \"supervisor shadow stack\" token on new shadow stack is marked busy. If destination mode is 32-bit or compatibility mode, but SSP address in \"supervisor shadow stack\" token is beyond 4GB. If SSP address in \"supervisor shadow stack\" token does not match SSP address in IA32_PLi_SSP (where i is the new CPL). If FRED event delivery would load RIP with a non-canonical address."
  },
  {
   "mode": "compat",
   "vector": "#SS(error_code)",
   "when": "If FRED event delivery would load SSP with an address that is not 8-byte aligned. If a shadow-stack access performed by FRED event delivery would use an address that is not"
  },
  {
   "mode": "compat",
   "vector": "#NP(error_code)",
   "when": "canonical relative to the current paging mode or cause a LASS violation."
  },
  {
   "mode": "compat",
   "vector": "#TS(error_code)",
   "when": "If an ordinary stack access would use an address that is not canonical relative to the current paging mode or cause a LASS violation."
  },
  {
   "mode": "compat",
   "vector": "#PF(fault-code)",
   "when": "If the 64-bit interrupt-gate, 64-bit trap-gate, or code segment is not present."
  },
  {
   "mode": "compat",
   "vector": "#UD",
   "when": "If an attempt to load RSP from the TSS causes an access to non-canonical space."
  },
  {
   "mode": "compat",
   "vector": "#AC(EXT)",
   "when": "If the RSP from the TSS is outside descriptor table limits. If a page fault occurs. If the LOCK prefix is used. If alignment checking is enabled, the gate DPL is 3, and a stack push is unaligned."
  },
  {
   "mode": "long",
   "vector": null,
   "when": "Same exceptions as in compatibility mode."
  }
 ],
 "flags": [
  "TF",
  "IF",
  "NT",
  "RF",
  "AC",
  "VM"
 ],
 "format": "isadoc",
 "id": "int-n",
 "isa": "x86",
 "links": {
  "arch_data": [
   "INT3"
  ]
 },
 "mnemonics": [
  "INT n",
  "INTO",
  "INT3",
  "INT1"
 ],
 "operand_encodings": [
  {
   "id": "I",
   "operands": [
    {
     "field": "imm8",
     "read": false,
     "write": false
    }
   ]
  }
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    1171,
    1186
   ]
  }
 ],
 "version": 1
}
