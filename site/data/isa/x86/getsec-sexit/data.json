{
 "encodings": [
  {
   "cpuid": "",
   "layout": {
    "opcode": [
     "0F",
     "37"
    ],
    "prefixes": [
     "NP"
    ]
   },
   "modes": {},
   "note": "Exit measured environment.",
   "opcode": "NP 0F 37",
   "operands": "",
   "syntax": "GETSEC[SEXIT]"
  },
  {
   "cpuid": "",
   "layout": {
    "leaf": {
     "register": "EAX",
     "value": "5"
    }
   },
   "modes": {},
   "note": "",
   "opcode": "(EAX=5)",
   "operands": "",
   "syntax": ""
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If CR4.SMXE = 0. If GETSEC[SEXIT] is not reported as supported by GETSEC[CAPABILITIES]."
  },
  {
   "mode": "protected",
   "vector": "#GP(0)",
   "when": "If CR0.PE = 0 or CPL > 0 or EFLAGS.VM = 1. If in VMX root operation. If the initiating processor is not designated via the MSR bit IA32_APIC_BASE.BSP. If an Intel(R) TXT-capable chipset is not present. If a protected partition is not already active or the processor is already in authenticated code mode. If the processor is in SMM."
  },
  {
   "mode": "real",
   "vector": "#UD",
   "when": "If CR4.SMXE = 0. If GETSEC[SEXIT] is not reported as supported by GETSEC[CAPABILITIES]."
  },
  {
   "mode": "real",
   "vector": "#GP(0)",
   "when": "GETSEC[SEXIT] is not recognized in real-address mode."
  },
  {
   "mode": "virtual8086",
   "vector": "#UD",
   "when": "If CR4.SMXE = 0. If GETSEC[SEXIT] is not reported as supported by GETSEC[CAPABILITIES]."
  },
  {
   "mode": "virtual8086",
   "vector": "#GP(0)",
   "when": "GETSEC[SEXIT] is not recognized in virtual-8086 mode."
  },
  {
   "mode": "compat",
   "vector": null,
   "when": "All protected mode exceptions apply."
  },
  {
   "mode": "long",
   "vector": null,
   "when": "All protected mode exceptions apply."
  }
 ],
 "format": "isadoc",
 "id": "getsec-sexit",
 "isa": "x86",
 "links": {
  "arch_data": []
 },
 "mnemonics": [
  "GETSEC[SEXIT]"
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    2931,
    2933
   ]
  }
 ],
 "version": 1
}
