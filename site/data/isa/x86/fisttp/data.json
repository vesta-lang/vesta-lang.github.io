{
 "encodings": [
  {
   "cpuid": "",
   "layout": {
    "modrm": {
     "kind": "digit",
     "value": 1
    },
    "opcode": [
     "DF"
    ]
   },
   "modes": {
    "legacy": "valid",
    "long": "valid"
   },
   "note": "Store ST(0) in m16int with truncation.",
   "opcode": "DF /1",
   "operands": "",
   "syntax": "FISTTP m16int"
  },
  {
   "cpuid": "",
   "layout": {
    "modrm": {
     "kind": "digit",
     "value": 1
    },
    "opcode": [
     "DB"
    ]
   },
   "modes": {
    "legacy": "valid",
    "long": "valid"
   },
   "note": "Store ST(0) in m32int with truncation.",
   "opcode": "DB /1",
   "operands": "",
   "syntax": "FISTTP m32int"
  },
  {
   "cpuid": "",
   "layout": {
    "modrm": {
     "kind": "digit",
     "value": 1
    },
    "opcode": [
     "DD"
    ]
   },
   "modes": {
    "legacy": "valid",
    "long": "valid"
   },
   "note": "Store ST(0) in m64int with truncation.",
   "opcode": "DD /1",
   "operands": "",
   "syntax": "FISTTP m64int"
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": "#GP(0)",
   "when": "If the destination is in a nonwritable segment. For an illegal memory operand effective address in the CS, DS, ES, FS or GS segments."
  },
  {
   "mode": "protected",
   "vector": "#SS(0)",
   "when": "For an illegal address in the SS segment. #PF(fault-code) For a page fault."
  },
  {
   "mode": "protected",
   "vector": "#AC(0)",
   "when": "If alignment checking is enabled and an unaligned memory reference is made while the current privilege level is 3."
  },
  {
   "mode": "protected",
   "vector": "#NM",
   "when": "If CR0.EM[bit 2] = 1. If CR0.TS[bit 3] = 1."
  },
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If CPUID.01H:ECX.SSE3[0] = 0. If the LOCK prefix is used. Real Address Mode Exceptions GP(0) If any part of the operand would lie outside of the effective address space from 0 to 0FFFFH."
  },
  {
   "mode": "protected",
   "vector": "#NM",
   "when": "If CR0.EM[bit 2] = 1. If CR0.TS[bit 3] = 1."
  },
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If CPUID.01H:ECX.SSE3[0] = 0. If the LOCK prefix is used. Virtual 8086 Mode Exceptions GP(0) If any part of the operand would lie outside of the effective address space from 0 to 0FFFFH."
  },
  {
   "mode": "protected",
   "vector": "#NM",
   "when": "If CR0.EM[bit 2] = 1. If CR0.TS[bit 3] = 1."
  },
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If CPUID.01H:ECX.SSE3[0] = 0. If the LOCK prefix is used. #PF(fault-code) For a page fault."
  },
  {
   "mode": "protected",
   "vector": "#AC(0)",
   "when": "For unaligned memory reference if the current privilege is 3."
  },
  {
   "mode": "compat",
   "vector": null,
   "when": "Same exceptions as in protected mode."
  },
  {
   "mode": "long",
   "vector": "#SS(0)",
   "when": "If a memory address referencing the SS segment is in a non-canonical form."
  },
  {
   "mode": "long",
   "vector": "#GP(0)",
   "when": "If the memory address is in a non-canonical form."
  },
  {
   "mode": "long",
   "vector": "#NM",
   "when": "CR0.EM[bit 2] or CR0.TS[bit 3] = 1."
  },
  {
   "mode": "long",
   "vector": "#MF",
   "when": "If there is a pending x87 FPU exception. #PF(fault-code) If a page fault occurs."
  },
  {
   "mode": "long",
   "vector": "#AC(0)",
   "when": "If alignment checking is enabled and an unaligned memory reference is made while the current privilege level is 3. If the LOCK prefix is used."
  }
 ],
 "format": "isadoc",
 "id": "fisttp",
 "isa": "x86",
 "links": {
  "arch_data": [
   "FISTTP"
  ]
 },
 "mnemonics": [
  "FISTTP"
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    1054,
    1055
   ]
  }
 ],
 "version": 1
}
