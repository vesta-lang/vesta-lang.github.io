---
summary: 未定义指令
---

## 说明

生成无效的 操作码 例外 。 本指令用于软件测试,以明确生成无效的操作码例外. 此指令的 操作码 保留用于此目的 。 除了提高无效的 操作码 例外外,本指令对处理器状态或内存没有任何影响. 尽管造成无效的操作码例外的是UD指令的执行,但指令指针通过发送例外引用UD指令(而不是以下指令)而保存. 此指令的操作在非64位模式和64位模式中是相同的.

## 行动

```text
#UD (* Generates invalid opcode exception *);
```

## 受影响的旗帜

None.
