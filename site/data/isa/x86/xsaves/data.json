{
 "encodings": [
  {
   "cpuid": "XSS",
   "layout": {
    "modrm": {
     "kind": "digit",
     "value": 5
    },
    "opcode": [
     "0F",
     "C7"
    ],
    "prefixes": [
     "NP"
    ]
   },
   "modes": {
    "legacy": "v",
    "long": "v"
   },
   "note": "Save state components specified by EDX:EAX to mem with compaction, optimizing if possible.",
   "opcode": "NP 0F C7 /5",
   "operands": "M",
   "syntax": "XSAVES mem"
  },
  {
   "cpuid": "XSS",
   "layout": {
    "modrm": {
     "kind": "digit",
     "value": 5
    },
    "opcode": [
     "0F",
     "C7"
    ],
    "prefixes": [
     "NP"
    ],
    "rex": "W"
   },
   "modes": {
    "legacy": "n.e.",
    "long": "v"
   },
   "note": "Save state components specified by EDX:EAX to mem with compaction, optimizing if possible.",
   "opcode": "NP REX.W + 0F C7 /5",
   "operands": "M",
   "syntax": "XSAVES64 mem"
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": "#GP(0)",
   "when": "If CPL > 0. If a memory operand effective address is outside the CS, DS, ES, FS, or GS segment limit. If a memory operand is not aligned on a 64-byte boundary, regardless of segment."
  },
  {
   "mode": "protected",
   "vector": "#SS(0)",
   "when": "If a memory operand effective address is outside the SS segment limit. #PF(fault-code) If a page fault occurs."
  },
  {
   "mode": "protected",
   "vector": "#NM",
   "when": "If CR0.TS[bit 3] = 1."
  },
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If CPUID.01H:ECX.XSAVE[26] = 0 or CPUID.0DH.01H:EAX.XSAVES[3] = 0. If CR4.OSXSAVE[bit 18] = 0. If the LOCK prefix is used."
  },
  {
   "mode": "real",
   "vector": "#GP",
   "when": "If a memory operand is not aligned on a 64-byte boundary, regardless of segment. If any part of the operand lies outside the effective address space from 0 to FFFFH."
  },
  {
   "mode": "real",
   "vector": "#NM",
   "when": "If CR0.TS[bit 3] = 1."
  },
  {
   "mode": "real",
   "vector": "#UD",
   "when": "If CPUID.01H:ECX.XSAVE[26] = 0 or CPUID.0DH.01H:EAX.XSAVES[3] = 0. If CR4.OSXSAVE[bit 18] = 0. If the LOCK prefix is used."
  },
  {
   "mode": "virtual8086",
   "vector": null,
   "when": "Same exceptions as in protected mode."
  },
  {
   "mode": "compat",
   "vector": null,
   "when": "Same exceptions as in protected mode."
  },
  {
   "mode": "long",
   "vector": "#GP(0)",
   "when": "If CPL > 0. If the memory address is in a non-canonical form. If a memory operand is not aligned on a 64-byte boundary, regardless of segment."
  },
  {
   "mode": "long",
   "vector": "#SS(0)",
   "when": "If a memory address referencing the SS segment is in a non-canonical form. #PF(fault-code) If a page fault occurs."
  },
  {
   "mode": "long",
   "vector": "#NM",
   "when": "If CR0.TS[bit 3] = 1."
  },
  {
   "mode": "long",
   "vector": "#UD",
   "when": "If CPUID.01H:ECX.XSAVE[26] = 0 or CPUID.0DH.01H:EAX.XSAVES[3] = 0. If CR4.OSXSAVE[bit 18] = 0. If the LOCK prefix is used."
  }
 ],
 "format": "isadoc",
 "id": "xsaves",
 "isa": "x86",
 "links": {
  "arch_data": [
   "XSAVES"
  ]
 },
 "mnemonics": [
  "XSAVES"
 ],
 "operand_encodings": [
  {
   "id": "M",
   "operands": [
    {
     "field": "modrm.rm",
     "read": false,
     "write": true
    }
   ]
  }
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    2894,
    2896
   ]
  }
 ],
 "version": 1
}
