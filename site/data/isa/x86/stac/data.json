{
 "encodings": [
  {
   "cpuid": "SMAP",
   "layout": {
    "opcode": [
     "0F",
     "01",
     "CB"
    ],
    "prefixes": [
     "NP"
    ]
   },
   "modes": {
    "legacy": "v",
    "long": "v"
   },
   "note": "Set the AC flag in the EFLAGS register.",
   "opcode": "NP 0F 01 CB",
   "operands": "ZO",
   "syntax": "STAC"
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If the CPL > 0. If CPUID.07H.00H:EBX.SMAP[20] = 0."
  },
  {
   "mode": "real",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If CPUID.07H.00H:EBX.SMAP[20] = 0."
  },
  {
   "mode": "virtual8086",
   "vector": "#UD",
   "when": "The STAC instruction is not recognized in virtual-8086 mode."
  },
  {
   "mode": "compat",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If the CPL > 0. If CPUID.07H.00H:EBX.SMAP[20] = 0."
  },
  {
   "mode": "long",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If the CPL > 0. If CPUID.07H.00H:EBX.SMAP[20] = 0."
  }
 ],
 "flags": [
  "AC"
 ],
 "format": "isadoc",
 "id": "stac",
 "isa": "x86",
 "links": {
  "arch_data": [
   "STAC"
  ]
 },
 "mnemonics": [
  "STAC"
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    1959,
    1959
   ]
  }
 ],
 "version": 1
}
