{
 "encodings": [
  {
   "cpuid": "SMAP",
   "layout": {
    "opcode": [
     "0F",
     "01",
     "CA"
    ],
    "prefixes": [
     "NP"
    ]
   },
   "modes": {
    "legacy": "v",
    "long": "v"
   },
   "note": "Clear the AC flag in the EFLAGS register.",
   "opcode": "NP 0F 01 CA",
   "operands": "ZO",
   "syntax": "CLAC"
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If the CPL > 0. If CPUID.07H.00H:EBX.SMAP[20] = 0."
  },
  {
   "mode": "real",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If CPUID.07H.00H:EBX.SMAP[20] = 0."
  },
  {
   "mode": "virtual8086",
   "vector": "#UD",
   "when": "The CLAC instruction is not recognized in virtual-8086 mode."
  },
  {
   "mode": "compat",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If the CPL > 0. If CPUID.07H.00H:EBX.SMAP[20] = 0."
  },
  {
   "mode": "long",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If the CPL > 0. If CPUID.07H.00H:EBX.SMAP[20] = 0."
  }
 ],
 "flags": [
  "AC"
 ],
 "format": "isadoc",
 "id": "clac",
 "isa": "x86",
 "links": {
  "arch_data": [
   "CLAC"
  ]
 },
 "mnemonics": [
  "CLAC"
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    843,
    843
   ]
  }
 ],
 "version": 1
}
