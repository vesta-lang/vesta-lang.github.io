---
summary: 以零扩展移动
---

## 说明

将 源操作数(注册人或内存位置)的内容复制到 目标操作数(注册人),零扩展值. 转换值的大小取决于操作数的大小属性.

在64位模式下,指令的默认操作大小为32位. 使用REX.R前缀可以访问额外的登记册(R8-R15). 使用REX.W前缀可以促进运行到64位操作数. 参见本节开头的汇总图,用于编码数据和限制.

## 行动

```text
DEST := ZeroExtend(SRC);
```

## 受影响的旗帜

None.
