{
 "encodings": [
  {
   "cpuid": "MPX",
   "layout": {
    "modrm": {
     "kind": "reg"
    },
    "opcode": [
     "0F",
     "1A"
    ],
    "prefixes": [
     "66"
    ]
   },
   "modes": {
    "legacy": "v",
    "long": "n.e."
   },
   "note": "Move lower and upper bound from bnd2/m64 to bound register bnd1.",
   "opcode": "66 0F 1A /r",
   "operands": "RM",
   "syntax": "BNDMOV bnd1, bnd2/m64"
  },
  {
   "cpuid": "MPX",
   "layout": {
    "modrm": {
     "kind": "reg"
    },
    "opcode": [
     "0F",
     "1A"
    ],
    "prefixes": [
     "66"
    ]
   },
   "modes": {
    "legacy": "n.e.",
    "long": "v"
   },
   "note": "Move lower and upper bound from bnd2/m128 to bound register bnd1.",
   "opcode": "66 0F 1A /r",
   "operands": "RM",
   "syntax": "BNDMOV bnd1, bnd2/m128"
  },
  {
   "cpuid": "MPX",
   "layout": {
    "modrm": {
     "kind": "reg"
    },
    "opcode": [
     "0F",
     "1B"
    ],
    "prefixes": [
     "66"
    ]
   },
   "modes": {
    "legacy": "v",
    "long": "n.e."
   },
   "note": "Move lower and upper bound from bnd2 to bnd1/m64.",
   "opcode": "66 0F 1B /r",
   "operands": "MR",
   "syntax": "BNDMOV bnd1/m64, bnd2"
  },
  {
   "cpuid": "MPX",
   "layout": {
    "modrm": {
     "kind": "reg"
    },
    "opcode": [
     "0F",
     "1B"
    ],
    "prefixes": [
     "66"
    ]
   },
   "modes": {
    "legacy": "n.e.",
    "long": "v"
   },
   "note": "Move lower and upper bound from bnd2 to bound register bnd1/m128.",
   "opcode": "66 0F 1B /r",
   "operands": "MR",
   "syntax": "BNDMOV bnd1/m128, bnd2"
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If the LOCK prefix is used but the destination is not a memory operand. If ModRM.r/m encodes BND4-BND7 when Intel MPX is enabled. If 67H prefix is not used and CS.D=0. If 67H prefix is used and CS.D=1."
  },
  {
   "mode": "protected",
   "vector": "#SS(0)",
   "when": "If the memory operand effective address is outside the SS segment limit."
  },
  {
   "mode": "protected",
   "vector": "#GP(0)",
   "when": "If the memory operand effective address is outside the CS, DS, ES, FS, or GS segment limit. If the destination operand points to a non-writable segment If the DS, ES, FS, or GS segment register contains a NULL segment selector."
  },
  {
   "mode": "protected",
   "vector": "#AC(0)",
   "when": "If alignment checking is enabled and an unaligned memory reference is made while CPL is 3. #PF(fault code) If a page fault occurs."
  },
  {
   "mode": "real",
   "vector": "#UD",
   "when": "If the LOCK prefix is used but the destination is not a memory operand. If ModRM.r/m encodes BND4-BND7 when Intel MPX is enabled. If 16-bit addressing is used."
  },
  {
   "mode": "real",
   "vector": "#GP(0)",
   "when": "If the memory operand effective address is outside the CS, DS, ES, FS, or GS segment limit."
  },
  {
   "mode": "real",
   "vector": "#SS",
   "when": "If the memory operand effective address is outside the SS segment limit."
  },
  {
   "mode": "virtual8086",
   "vector": "#UD",
   "when": "If the LOCK prefix is used but the destination is not a memory operand. If ModRM.r/m encodes BND4-BND7 when Intel MPX is enabled. If 16-bit addressing is used."
  },
  {
   "mode": "virtual8086",
   "vector": "#GP(0)",
   "when": "If the memory operand effective address is outside the CS, DS, ES, FS, or GS segment limit."
  },
  {
   "mode": "virtual8086",
   "vector": "#SS(0)",
   "when": "If the memory operand effective address is outside the SS segment limit."
  },
  {
   "mode": "virtual8086",
   "vector": "#AC(0)",
   "when": "If alignment checking is enabled and an unaligned memory reference is made while CPL is 3. #PF(fault code) If a page fault occurs."
  },
  {
   "mode": "compat",
   "vector": null,
   "when": "Same exceptions as in protected mode."
  },
  {
   "mode": "long",
   "vector": "#UD",
   "when": "If the LOCK prefix is used but the destination is not a memory operand. If ModRM.r/m and REX encodes BND4-BND15 when Intel MPX is enabled."
  },
  {
   "mode": "long",
   "vector": "#SS(0)",
   "when": "If the memory address referencing the SS segment is in a non-canonical form."
  },
  {
   "mode": "long",
   "vector": "#GP(0)",
   "when": "If the memory address is in a non-canonical form."
  },
  {
   "mode": "long",
   "vector": "#AC(0)",
   "when": "If alignment checking is enabled and an unaligned memory reference is made while CPL is 3. #PF(fault code) If a page fault occurs."
  }
 ],
 "format": "isadoc",
 "id": "bndmov",
 "isa": "x86",
 "links": {
  "arch_data": [
   "BNDMOV"
  ]
 },
 "mnemonics": [
  "BNDMOV"
 ],
 "operand_encodings": [
  {
   "id": "RM",
   "operands": [
    {
     "field": "modrm.reg",
     "read": false,
     "write": true
    },
    {
     "field": "modrm.rm",
     "read": true,
     "write": false
    }
   ]
  },
  {
   "id": "MR",
   "operands": [
    {
     "field": "modrm.rm",
     "read": false,
     "write": true
    },
    {
     "field": "modrm.reg",
     "read": true,
     "write": false
    }
   ]
  }
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    803,
    805
   ]
  }
 ],
 "version": 1
}
