{
 "encodings": [
  {
   "cpuid": "",
   "layout": {
    "opcode": [
     "F0"
    ]
   },
   "modes": {
    "legacy": "valid",
    "long": "valid"
   },
   "note": "Asserts LOCK# signal for duration of the accompanying instruction.",
   "opcode": "F0",
   "operands": "ZO",
   "syntax": "LOCK"
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If the LOCK prefix is used with an instruction not listed: ADD, ADC, AND, BTC, BTR, BTS, CMPXCHG, CMPXCH8B, CMPXCHG16B, DEC, INC, NEG, NOT, OR, SBB, SUB, XOR, XADD, XCHG. Other exceptions can be generated by the instruction when the LOCK prefix is applied."
  },
  {
   "mode": "real",
   "vector": null,
   "when": "Same exceptions as in protected mode."
  },
  {
   "mode": "virtual8086",
   "vector": null,
   "when": "Same exceptions as in protected mode."
  },
  {
   "mode": "compat",
   "vector": null,
   "when": "Same exceptions as in protected mode."
  },
  {
   "mode": "long",
   "vector": null,
   "when": "Same exceptions as in protected mode."
  }
 ],
 "format": "isadoc",
 "id": "lock",
 "isa": "x86",
 "links": {
  "arch_data": []
 },
 "mnemonics": [
  "LOCK"
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    1269,
    1270
   ]
  }
 ],
 "version": 1
}
