{
 "encodings": [
  {
   "cpuid": "SSE2",
   "layout": {
    "modrm": {
     "kind": "reg"
    },
    "opcode": [
     "0F",
     "C3"
    ],
    "prefixes": [
     "NP"
    ]
   },
   "modes": {
    "legacy": "v",
    "long": "v"
   },
   "note": "Move doubleword from r32 to m32 using non-temporal hint.",
   "opcode": "NP 0F C3 /r",
   "operands": "MR",
   "syntax": "MOVNTI m32, r32"
  },
  {
   "cpuid": "SSE2",
   "layout": {
    "modrm": {
     "kind": "reg"
    },
    "opcode": [
     "0F",
     "C3"
    ],
    "prefixes": [
     "NP"
    ],
    "rex": "W"
   },
   "modes": {
    "legacy": "n.e.",
    "long": "v"
   },
   "note": "Move quadword from r64 to m64 using non-temporal hint.",
   "opcode": "NP REX.W + 0F C3 /r",
   "operands": "MR",
   "syntax": "MOVNTI m64, r64"
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": "#GP(0)",
   "when": "For an illegal memory operand effective address in the CS, DS, ES, FS or GS segments."
  },
  {
   "mode": "protected",
   "vector": "#SS(0)",
   "when": "For an illegal address in the SS segment. #PF(fault-code) For a page fault."
  },
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If CPUID.01H:EDX.SSE2[26] = 0. If the LOCK prefix is used."
  },
  {
   "mode": "real",
   "vector": "#GP",
   "when": "If any part of the operand lies outside the effective address space from 0 to FFFFH."
  },
  {
   "mode": "real",
   "vector": "#UD",
   "when": "If CPUID.01H:EDX.SSE2[26] = 0. If the LOCK prefix is used."
  },
  {
   "mode": "virtual8086",
   "vector": null,
   "when": "Same exceptions as in real address mode."
  },
  {
   "mode": "compat",
   "vector": null,
   "when": "Same exceptions as in protected mode."
  },
  {
   "mode": "long",
   "vector": "#SS(0)",
   "when": "If a memory address referencing the SS segment is in a non-canonical form."
  },
  {
   "mode": "long",
   "vector": "#GP(0)",
   "when": "If the memory address is in a non-canonical form. #PF(fault-code) For a page fault."
  },
  {
   "mode": "long",
   "vector": "#UD",
   "when": "If CPUID.01H:EDX.SSE2[26] = 0. If the LOCK prefix is used."
  },
  {
   "mode": "long",
   "vector": "#AC(0)",
   "when": "If alignment checking is enabled and an unaligned memory reference is made while the current privilege level is 3."
  }
 ],
 "format": "isadoc",
 "id": "movnti",
 "isa": "x86",
 "links": {
  "arch_data": [
   "MOVNTI"
  ]
 },
 "mnemonics": [
  "MOVNTI"
 ],
 "operand_encodings": [
  {
   "id": "MR",
   "operands": [
    {
     "field": "modrm.rm",
     "read": false,
     "write": true
    },
    {
     "field": "modrm.reg",
     "read": true,
     "write": false
    }
   ]
  }
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    1382,
    1383
   ]
  }
 ],
 "version": 1
}
