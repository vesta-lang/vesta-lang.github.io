{
 "encodings": [
  {
   "cpuid": "CET_SS",
   "layout": {
    "modrm": {
     "kind": "digit",
     "value": 6
    },
    "opcode": [
     "0F",
     "AE"
    ],
    "prefixes": [
     "F3"
    ]
   },
   "modes": {
    "legacy": "v",
    "long": "v"
   },
   "note": "Clear busy flag in supervisor shadow stack token reference by m64.",
   "opcode": "F3 0F AE /6",
   "operands": "M",
   "syntax": "CLRSSBSY m64"
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If CR4.CET = 0. IF IA32_S_CET.SH_STK_EN = 0."
  },
  {
   "mode": "protected",
   "vector": "#GP(0)",
   "when": "If memory operand linear address not aligned to 8 bytes. If a memory operand effective address is outside the CS, DS, ES, FS, or GS segment limit. If destination is located in a non-writable segment. If the DS, ES, FS, or GS register is used to access memory and it contains a NULL segment selector. If CPL is not 0."
  },
  {
   "mode": "protected",
   "vector": "#SS(0)",
   "when": "If a memory operand effective address is outside the SS segment limit. #PF(fault-code) If a page fault occurs."
  },
  {
   "mode": "real",
   "vector": "#UD",
   "when": "The CLRSSBSY instruction is not recognized in real-address mode."
  },
  {
   "mode": "virtual8086",
   "vector": "#UD",
   "when": "The CLRSSBSY instruction is not recognized in virtual-8086 mode."
  },
  {
   "mode": "compat",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If CR4.CET = 0. IF IA32_S_CET.SH_STK_EN = 0. If CR4.FRED = 1."
  },
  {
   "mode": "compat",
   "vector": "#GP(0)",
   "when": "If memory operand linear address not aligned to 8 bytes. If a memory operand effective address is outside the CS, DS, ES, FS, or GS segment limit. If destination is located in a non-writable segment. If the DS, ES, FS, or GS register is used to access memory and it contains a NULL segment selector. If CPL is not 0."
  },
  {
   "mode": "compat",
   "vector": "#SS(0)",
   "when": "If a memory operand effective address is outside the SS segment limit. #PF(fault-code) If a page fault occurs."
  },
  {
   "mode": "long",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If CR4.CET = 0. If CR4.FRED = 1. IF IA32_S_CET.SH_STK_EN = 0."
  },
  {
   "mode": "long",
   "vector": "#GP(0)",
   "when": "If memory operand linear address not aligned to 8 bytes. If CPL is not 0. If the memory address is in a non-canonical form."
  },
  {
   "mode": "long",
   "vector": "#SS(0)",
   "when": "If a memory address referencing the SS segment is in a non-canonical form. #PF(fault-code) If a page fault occurs."
  }
 ],
 "flags": [
  "CF",
  "PF",
  "AF",
  "ZF",
  "SF",
  "OF"
 ],
 "format": "isadoc",
 "id": "clrssbsy",
 "isa": "x86",
 "links": {
  "arch_data": [
   "CLRSSBSY"
  ]
 },
 "mnemonics": [
  "CLRSSBSY"
 ],
 "operand_encodings": [
  {
   "id": "M",
   "operands": [
    {
     "field": "modrm.rm",
     "read": true,
     "write": true
    }
   ]
  }
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    854,
    855
   ]
  }
 ],
 "version": 1
}
