{
 "encodings": [
  {
   "cpuid": "MPX",
   "layout": {
    "modrm": {
     "kind": "reg"
    },
    "opcode": [
     "0F",
     "1B"
    ],
    "prefixes": [
     "NP"
    ]
   },
   "modes": {
    "legacy": "v",
    "long": "v"
   },
   "note": "Store the bounds in bnd and the pointer value in the index register of mib to a bound table entry (BTE) with address translation using the base of mib.",
   "opcode": "NP 0F 1B /r",
   "operands": "MR",
   "syntax": "BNDSTX mib, bnd"
  }
 ],
 "exceptions": [
  {
   "mode": "protected",
   "vector": "#BR",
   "when": "If the bound directory entry is invalid."
  },
  {
   "mode": "protected",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If ModRM.r/m encodes BND4-BND7 when Intel MPX is enabled. If 67H prefix is not used and CS.D=0. If 67H prefix is used and CS.D=1."
  },
  {
   "mode": "protected",
   "vector": "#GP(0)",
   "when": "If a destination effective address of the Bound Table entry is outside the DS segment limit. If DS register contains a NULL segment selector. If the destination operand points to a non-writable segment #PF(fault code) If a page fault occurs."
  },
  {
   "mode": "real",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If ModRM.r/m encodes BND4-BND7 when Intel MPX is enabled. If 16-bit addressing is used."
  },
  {
   "mode": "real",
   "vector": "#GP(0)",
   "when": "If a destination effective address of the Bound Table entry is outside the DS segment limit."
  },
  {
   "mode": "virtual8086",
   "vector": "#UD",
   "when": "If the LOCK prefix is used. If ModRM.r/m encodes BND4-BND7 when Intel MPX is enabled. If 16-bit addressing is used."
  },
  {
   "mode": "virtual8086",
   "vector": "#GP(0)",
   "when": "If a destination effective address of the Bound Table entry is outside the DS segment limit. #PF(fault code) If a page fault occurs."
  },
  {
   "mode": "compat",
   "vector": null,
   "when": "Same exceptions as in protected mode."
  },
  {
   "mode": "long",
   "vector": "#BR",
   "when": "If the bound directory entry is invalid."
  },
  {
   "mode": "long",
   "vector": "#UD",
   "when": "If ModRM is RIP relative. If the LOCK prefix is used. If ModRM.r/m and REX encodes BND4-BND15 when Intel MPX is enabled."
  },
  {
   "mode": "long",
   "vector": "#GP(0)",
   "when": "If the memory address (A_BDE or A_BTE) is in a non-canonical form. If the destination operand points to a non-writable segment #PF(fault code) If a page fault occurs."
  }
 ],
 "format": "isadoc",
 "id": "bndstx",
 "isa": "x86",
 "links": {
  "arch_data": [
   "BNDSTX"
  ]
 },
 "mnemonics": [
  "BNDSTX"
 ],
 "operand_encodings": [
  {
   "id": "MR",
   "operands": [
    {
     "field": "SIB.base (r): Address of pointer",
     "raw": "SIB.base (r): Address of pointer",
     "read": false,
     "write": false
    },
    {
     "field": "modrm.reg",
     "read": true,
     "write": false
    }
   ]
  }
 ],
 "sources": [
  {
   "edition": "325462-091US",
   "id": "intel-sdm",
   "pages": [
    806,
    808
   ]
  }
 ],
 "version": 1
}
